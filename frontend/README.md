# Frontend (Next.js)

Placeholder frontend for the WalkScore application.

## Setup

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:3000` and proxies `/api/*` requests to the Flask backend on port 5000 (see `next.config.js`).
